import { ChaincodeTx } from '@worldsibu/convector-platform-fabric';
import {
  Controller,
  ConvectorController,
  Invokable,
  Param
} from '@worldsibu/convector-core';

import { <%= className %> } from './<%= name %>.model';

@Controller('<%= name %>')
export class <%= className %>Controller extends ConvectorController<ChaincodeTx> {
  @Invokable()
  public async create(
    @Param(<%= className %>)
    <%= varName %>: <%= className %>
  ) {
    await <%= varName %>.save();
  }
}